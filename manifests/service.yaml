apiVersion: v1
kind: Service
metadata:
  name: web-app-service
  labels:
    app.kubernetes.io/name: web-app
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  selector:
    app.kubernetes.io/name: web-app
